Array :
    if we get array on stack we cant findout what size is actually is
    because it stored on stack and we should using sizeof function
    if we want count of element we should divide size of it to size of each element it has
    